{
  "name": "kss-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron desktop shell for Karaoke Subtitle Studio",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "smoke": "electron . --smoke --no-sandbox",
    "test": "node --test"
  },
  "devDependencies": {
    "electron": "^42.3.3"
  }
}
